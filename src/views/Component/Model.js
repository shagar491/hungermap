import { useState } from "react";

function Model() {
    const [showModel, setShowModel] = useState(false)

    const handleClick = () => {
        setShowModel(true)
    }

    return (<>
        <section>

        </section>
    </>)
}

export default Model;